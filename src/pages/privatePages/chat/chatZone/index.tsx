import { styled } from 'styled-components'
import tw from 'twin.macro'

const ChatZone: React.FC = () => {
  return <Container>This is chat Zone</Container>
}
export default ChatZone

const Container = styled.div`
  ${tw`flex-1 h-full`}
`
